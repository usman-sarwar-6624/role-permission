import React from 'react'
import LoginView from '../../../views/Role/ListView'

export default function RolesList(props) {
    return (
        <div>
        <ListView props={props} />
        </div>
    )
}
